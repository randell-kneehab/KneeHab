.DS_Store
*.log
node_modules/
.env
.env.local
.idea/
.vscode/

# Large local video previews — host on a CDN for production
videos/*
# ...except the bands hero video, which ships with the repo
!videos/bands-hero.mp4

# Wrangler local state (R2/KV/D1 caches) — never commit
.wrangler/

.dev.vars*
!.dev.vars.example
!.env.example
